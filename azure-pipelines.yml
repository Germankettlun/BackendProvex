# =========================
# Orquestador - Llama al pipeline real en /Pipelines
# =========================
# Este archivo solo sirve como punto de entrada (trigger)
# La lógica real está en: ProvexBackendAPI/Pipelines/azure-pipelines-backend.yml
# =========================

trigger:
  branches:
    include:
      - dev
      - prod

pr:
  branches:
    include:
      - dev
      - prod

# =========================
# EXTENDS: Llamar al pipeline modular
# =========================
extends:
  template: ProvexBackendAPI/Pipelines/azure-pipelines-backend.yml
