# =========================
# Orquestador - Llama al pipeline real en /pipelines
# =========================
# Este archivo solo sirve como punto de entrada (trigger)
# La lógica real está en: pipelines/azure-pipelines-backend.yml
# =========================

trigger:
  branches:
    include:
      - dev
      - prod

pr:
  branches:
    include:
      - dev
      - prod

# =========================
# EXTENDS: Llamar al pipeline modular
# =========================
extends:
  template: pipelines/azure-pipelines-backend.yml